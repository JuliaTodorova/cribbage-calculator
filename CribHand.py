import Hand


class CribHand(Hand):

    def __init__(self, cards, values, suit, colour):
        super().self._cards = cards
        super().self._values = values
        super().self._suit = suit
        super().self._colour = colour
